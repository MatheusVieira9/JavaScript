let num = [5, 8, 6, 7, 2]
/*
num.sort() // coloca os elementos do vetor em ordem.
num.push(0) // adiciona um elemento no final do vetor.
console.log(num)
console.log(`O vetor tem ${num.length} posições.`)
console.log(`O primeiro valor do vetor é ${num[0]}`)

for(let chave = 0; chave < num.length; chave++){
    console.log(`Posição ${chave} do vetor representa o valor ${num[chave]}`)
}
for(let chave in num){
    console.log(`Posição ${chave} do vetor representa o valor ${num[chave]}`)
}

*/
console.log(`O valor 6 é o ${num.indexOf(6) + 1}º item do vetor.`) // indexOf() procura o valor dentro do vetor.